<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Set Custom Font -->
        <item name="android:typeface">monospace</item>
    </style>

    <style name="Text_Heading">
        <item name="android:textSize">@dimen/text_size_heading</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Text_Normal">
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textColor">@color/color_header_text</item>
        <item name="android:textColorHint">@color/color_hint_text</item>
        <item name="android:inputType">textCapWords</item>
    </style>

    <!--<style name="careclusRatingBar" parent="@android:style/Widget.RatingBar">-->
        <!--<item name="colorAccent">@color/colorPrimary</item>-->
    <!--</style>-->

    <style name="customRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/customratingstars</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:maxHeight">30dp</item>
    </style>

    <style name="customRatingBarSamll" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/customratingstars</item>
    </style>

    <style name="transparentDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
